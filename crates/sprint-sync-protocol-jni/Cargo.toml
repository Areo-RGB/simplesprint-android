[package]
name = "sprint-sync-protocol-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
serde_json = "1"
sprint-sync-protocol = { path = "../sprint-sync-protocol" }
